<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.footoo.ting"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />
    <permission android:name="android.permission.INTERNET"></permission>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".HotPageActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainBaseActivity" >
        </activity>
        <activity android:name=".AllPageActivity" >
        </activity>
        <activity android:name=".MyFavoActivity" >
        </activity>
        <activity android:name=".HistoryPageActivity" >
        </activity>
        <activity android:name=".LocalSourceActivity" >
        </activity>

        
        <service android:name="org.footoo.ting.media.PlayerEngine"></service>
    </application>

</manifest>